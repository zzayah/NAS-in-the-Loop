[project]
name = "f1tenth-ng"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
docs = [
  "sphinx>=7",
  "myst-parser>=4.0.1",
  "sphinx-markdown-builder",
  "furo",
]
test = [
  "pytest",
  "pytest-mock",
  "pylint",
]

[tool.setuptools]
packages = []
